name: Build feat-flow dist

on:
  push:
    branches: [main]
    paths:
      - 'plugins/feat-flow/src/**'
      - 'plugins/feat-flow/tsconfig*.json'
      - 'plugins/feat-flow/package.json'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: plugins/feat-flow/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: plugins/feat-flow

      - name: Build
        run: npm run build
        working-directory: plugins/feat-flow

      - name: Commit dist if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add plugins/feat-flow/dist/ --force
          if git diff --cached --quiet; then
            echo "dist/ unchanged, nothing to commit"
          else
            git commit -m "build(feat-flow): compile dist/ [auto]"
            git push
          fi
