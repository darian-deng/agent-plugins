/**
 * Read the last assistant entry's token usage from the session JSONL transcript.
 * Returns total input tokens (including cache).
 */
export declare function readTokenCount(sessionId: string, cwd: string): number;
export declare function contextPct(sessionId: string, cwd: string, contextSize: number): number;
//# sourceMappingURL=context.d.ts.map