#!/bin/sh
# feat-flow preflight checks
# This script runs once when 'feat-flow start' is called.
# Exit 0 = checks passed. Non-zero = blocked with error message.
# Customize for your project's requirements.

# Example: verify git is available
# command -v git >/dev/null 2>&1 || { echo "git is required"; exit 1; }

# Example: verify node version
# node --version >/dev/null 2>&1 || { echo "node is required"; exit 1; }

exit 0
